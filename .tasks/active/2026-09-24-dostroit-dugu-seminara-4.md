# Доиграть в этом репозитории кейсы Семинара 4, которые семинар называет сыгранными

- **Заведено:** 2026-09-24T05:10:48Z
- **Сверено:** 2026-09-24T05:31:36Z
- **Issue / PR:** нет issue; работа идёт на ветке `seminar-4-arc`, `main` не трогается

## Цель

Каждое состояние репозитория, которое слайд Семинара 4 показывает как настоящее, должно
существовать в настоящем коммите этого репозитория, с настоящим прогоном за ним. Не
«похоже на слайд» — а «слайд можно проверить командой».

## Как проверю

- `npm run build` завершается кодом 0 на каждом коммите дуги.
- `npx playwright test` зелёный на каждом коммите дуги.
- `grep -r 'CLAUDE.md' --include='*' ` по дереву не находит ни `tests/CLAUDE.md`, ни
  `src/CLAUDE.md` (целевой ответ кейса 1.3 — вложенных файлов инструкций нет).
- Каждая ссылка из кода и из ADR на файл в репозитории ведёт на существующий файл.
- Обещание комментария к `submitForm()` («ровно один повтор») ломается мутацией и тест
  это ловит — иначе комментарий ничем не удерживается.

## Шаги

- [x] Кейс 1.2 — гейт готовности текстом, без хука (s19/s56) — **сделан**, коммит `1019546`
- [x] Кейс 1.3 — `submitForm()` с повтором + комментарий, объект страницы, три теста
      доставки — **сделан**, коммит `7d0fd16`
- [x] Кейс 2.1 — первая содержательная запись в `DECISIONS.md` — **сделан**, коммит `90cbaf7`
- [x] Кейс 2.2 — `doc/adr/` по сигналу, три настоящих решения — **сделан**, коммит `d85f26c`
- [ ] Кейс 2.3 — практика файла-на-задачу + блок в `CLAUDE.md` — **в работе** (этот файл и есть он)
- [ ] README дуги переписать под то, что реально сыграно — **не начат**
- [x] Ветка `seminar-5-hook` — хук вернуть с настоящим прогоном самотеста — **сделан**
- [ ] Решение оператора: мержить ли `seminar-4-arc` в `main` — **заблокировано на
      операторе**. Дуга растёт прямо из `c0b1a47`, поэтому мерж — fast-forward, без
      переписывания истории. Действие за оператором, не за мной

## Что уже проверено

Дословно, на коммите `d85f26c`:

```
$ npm run build
dist/index.html                 1.39 kB │ gzip: 0.74 kB
dist/assets/index-BCI1ZQDc.js   2.59 kB │ gzip: 1.37 kB
✓ built in 84ms

$ npx playwright test
  7 passed (3.1s)
```

Мутационная проверка теста на повтор (`MAX_ATTEMPTS` 2 → 3):

```
  1 failed
    tests/delivery.spec.ts:29:1 › a request that keeps timing out is not retried a third time
    expected ['POST','POST'], got ['POST','POST','POST']
```

Мутация откачена, базовый прогон снова `7 passed`.

## Открытые вопросы / блокировки

- **Оператор** — делать ли `seminar-4-arc` ветку `main`. Пока не решено, `main` остаётся
  как был; force-push по публичной истории необратим, поэтому решение не моё.
- **Автор ветки семинара** — s20 говорит «тринадцать строк», в его блоке 12, а настоящий
  файл в этом репозитории 15. Слайд следует за репозиторием; правка не моя, вынесена в
  находки.
- Дата записи в `DECISIONS.md` — реальная (2026-09-24), а не 2026-09-26 с s35: той даты на
  момент записи ещё не было.
